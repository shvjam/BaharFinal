# مستندات صفحه مدیریت خدمات (AdminServices.tsx)

## 📋 خلاصه
صفحه جامع مدیریت خدمات باربری بهار که امکان مدیریت کامل ۷ نوع خدمت اسباب‌کشی را فراهم می‌کند. این صفحه شامل لیست خدمات، فرم افزودن/ویرایش چندمرحله‌ای، صفحه جزئیات، آمار عملکرد، و قابلیت‌های مدیریتی کامل است.

## 🎯 امکانات اصلی

### 1. کارت‌های آماری (Stats Cards)
چهار کارت آماری در بالای صفحه:

#### کل خدمات:
- نمایش تعداد کل خدمات
- تعداد خدمات فعال و غیرفعال
- آیکون: Package

#### خدمات ویژه:
- تعداد خدماتی که در صفحه اصلی نمایش داده می‌شوند
- توضیحات: "در صفحه اصلی نمایش داده می‌شود"
- آیکون: Activity

#### کل سفارشات:
- مجموع سفارشات تمام خدمات
- نمایش با فرمت فارسی (با جداکننده)
- آیکون: BarChart3

#### کل درآمد:
- مجموع درآمد تمام خدمات (به میلیون تومان)
- میانگین امتیاز کلی
- آیکون: DollarSign

### 2. فیلتر و جستجو
بخش جامع فیلتر شامل:

#### جستجو:
- جستجو بر اساس نام خدمت
- جستجو در توضیحات
- جستجو در slug
- آیکون جستجو در سمت راست Input

#### فیلتر وضعیت:
- **همه خدمات**: نمایش تمام خدمات
- **فعال**: فقط خدمات فعال
- **غیرفعال**: فقط خدمات غیرفعال
- **ویژه**: فقط خدمات ویژه

#### دکمه‌های عملیاتی:
- دانلود (Export): خروجی اطلاعات
- فیلتر پیشرفته: فیلترهای بیشتر

### 3. جدول لیست خدمات
جدول جامع با ستون‌های زیر:

#### ستون خدمت:
- آیکون خدمت (ایموجی بزرگ در مربع)
- نام خدمت
- Slug خدمت (زیر نام، با فونت کوچکتر)

#### ستون قیمت پایه:
- قیمت به میلیون تومان
- بج تخفیف (اگر دارد) با رنگ قرمز

#### ستون آمار:
- تعداد کل سفارشات
- تعداد سفارشات تکمیل شده (زیر کل، با فونت کوچکتر)

#### ستون امتیاز:
- ستاره زرد + عدد امتیاز

#### ستون درآمد:
- مجموع درآمد به میلیون تومان با جداکننده فارسی

#### ستون وضعیت:
- بج فعال/غیرفعال
- بج "ویژه" (اگر خدمت ویژه باشد) با بردر زرد

#### ستون عملیات:
منوی Dropdown شامل:
- **مشاهده جزئیات**: نمایش دیالوگ جزئیات کامل
- **ویرایش**: باز کردن فرم ویرایش
- **فعال/غیرفعال کردن**: تغییر وضعیت
- **افزودن/حذف از ویژه**: تغییر وضعیت ویژه
- **حذف**: حذف خدمت (با رنگ قرمز)

### 4. فرم افزودن/ویرایش خدمت
فرم چندتبه (۴ تب) با امکانات کامل:

#### تب 1: اطلاعات پایه
- **نام خدمت** (اجباری): نام فارسی خدمت
- **Slug** (اجباری): شناسه یکتا انگلیسی (مثل: moving-service)
- **آیکون** (اجباری): ایموجی خدمت
- **توضیحات کوتاه**: یک خط توضیح مختصر
- **توضیحات کامل** (اجباری): Textarea با 4 خط
- **تصویر خدمت**: آپلود تصویر با کامپوننت ImageUpload

#### تب 2: قیمت‌گذاری
- **قیمت پایه** (اجباری): قیمت پایه به تومان
- **قیمت هر کیلومتر**: نرخ هر کیلومتر (اختیاری)
- **درصد تخفیف**: عدد بین 0 تا 100 (%)
- **ترتیب نمایش**: اولویت نمایش در لیست
- **حداقل قیمت**: کمترین قیمت ممکن
- **حداکثر قیمت**: بیشترین قیمت ممکن

#### تب 3: ویژگی‌ها
- لیست داینامیک ویژگی‌ها
- دکمه "افزودن ویژگی" برای اضافه کردن ردیف جدید
- هر ویژگی یک Input جداگانه دارد
- دکمه حذف برای هر ویژگی (اگر بیش از یک ویژگی باشد)
- پیش‌نمایش ویژگی‌ها به صورت لیست Bullet

#### تب 4: تنظیمات
- **وضعیت فعال/غیرفعال**: Switch با توضیحات
- **خدمت ویژه**: Switch برای نمایش در صفحه اصلی
- **یادداشت ادمین**: Textarea برای یادداشت‌های داخلی

### 5. دیالوگ جزئیات خدمت
صفحه کامل نمایش اطلاعات شامل:

#### Header:
- آیکون بزرگ خدمت (64x64 پیکسل)
- نام خدمت
- Slug
- بج‌های وضعیت (فعال/غیرفعال، ویژه)

#### آمار عملکرد:
۴ کارت جداگانه:
- **کل سفارشات**: تعداد کل
- **تکمیل شده**: تعداد + درصد نرخ تکمیل
- **امتیاز**: عدد + ستاره
- **کل درآمد**: به میلیون تومان

#### توضیحات:
- نمایش توضیحات کامل خدمت

#### قیمت‌گذاری:
- قیمت پایه
- قیمت هر کیلومتر (اگر دارد)
- درصد تخفیف (اگر دارد)
- حداقل و حداکثر قیمت (اگر دارد)

#### ویژگی‌ها:
- لیست تمام ویژگی‌ها به صورت Bullet List

#### یادداشت ادمین:
- نمایش یادداشت (اگر دارد) در باکس خاکستری

#### تاریخ‌ها:
- تاریخ ایجاد
- آخرین بروزرسانی (اگر دارد)

### 6. دیالوگ حذف
- نمایش نام خدمت
- هشدار عدم بازگشت‌پذیری
- دکمه‌های انصراف و حذف

## 📊 ساختار داده

### Interface Service
```typescript
interface Service {
  id: string;                    // شناسه یکتا
  name: string;                  // نام فارسی
  slug: string;                  // شناسه URL
  description: string;           // توضیحات کامل
  shortDescription?: string;     // توضیح کوتاه
  icon: string;                  // ایموجی
  imageUrl?: string;             // آدرس تصویر
  
  // قیمت‌گذاری
  basePrice: number;             // قیمت پایه
  pricePerKm?: number;           // قیمت هر کیلومتر
  discountPercentage?: number;   // درصد تخفیف
  
  // ویژگی‌ها
  features: string[];            // لیست ویژگی‌ها
  
  // تنظیمات
  isActive: boolean;             // فعال/غیرفعال
  isFeatured: boolean;           // ویژه
  order: number;                 // ترتیب نمایش
  minPrice?: number;             // حداقل قیمت
  maxPrice?: number;             // حداکثر قیمت
  
  // آمار
  totalOrders: number;           // کل سفارشات
  completedOrders: number;       // سفارشات تکمیل شده
  totalRevenue: number;          // کل درآمد
  averageRating: number;         // میانگین امتیاز
  
  // متا
  createdAt: Date;               // تاریخ ایجاد
  updatedAt?: Date;              // آخرین بروزرسانی
  adminNote?: string;            // یادداشت ادمین
}
```

## 🎨 ویژگی‌های UI/UX

### بج‌ها (Badges):
- **فعال**: variant="default" (آبی/سبز)
- **غیرفعال**: variant="secondary" (خاکستری)
- **ویژه**: variant="outline" با بردر زرد و متن زرد
- **تخفیف**: variant="destructive" (قرمز)

### رنگ‌بندی:
- عملیات حذف: text-red-600
- ستاره امتیاز: text-yellow-500
- متن کمک‌رسان: text-muted-foreground

### آیکون‌ها:
از lucide-react استفاده شده:
- Plus, Search, Filter, MoreVertical
- Edit, Trash2, Eye
- Package, DollarSign, TrendingUp, Activity
- CheckCircle2, XCircle, Settings
- Upload, Download, Image
- BarChart3, Calendar, Users

### Responsive:
- کارت‌های آمار: 4 ستون در دسکتاپ، تک ستون در موبایل
- فیلترها: ردیف در دسکتاپ، ستون در موبایل
- فرم‌ها: 2 ستون در دسکتاپ، تک ستون در موبایل

## 🔄 عملیات (Handlers)

### handleAddService:
- ریست کردن فرم به مقادیر پیش‌فرض
- باز کردن دیالوگ افزودن
- ترتیب پیش‌فرض = تعداد خدمات + 1

### handleEditService:
- پر کردن فرم با اطلاعات خدمت انتخاب شده
- باز کردن دیالوگ ویرایش

### handleSaveService:
- چک کردن حالت (افزودن یا ویرایش)
- فیلتر کردن ویژگی‌های خالی
- به‌روزرسانی updatedAt
- نمایش toast موفقیت

### confirmDeleteService:
- حذف خدمت از لیست
- نمایش toast موفقیت

### toggleServiceStatus:
- تغییر isActive
- به‌روزرسانی updatedAt
- نمایش toast

### toggleFeaturedStatus:
- تغییر isFeatured
- به‌روزرسانی updatedAt
- نمایش toast

### addFeature:
- اضافه کردن یک ویژگی خالی به لیست

### removeFeature:
- حذف ویژگی از لیست بر اساس index

### updateFeature:
- به‌روزرسانی متن ویژگی در index مشخص

## 📦 Mock Data

فایل شامل 7 خدمت نمونه:

1. **اسباب‌کشی سریع، مطمئن و آسان**: 
   - قیمت پایه: 2 میلیون
   - 1250 سفارش
   - فعال و ویژه
   
2. **کارگر**:
   - قیمت پایه: 900 هزار
   - 850 سفارش
   - فعال و ویژه

3. **بسته‌بند یا فنی یا کارگر حمل خالی**:
   - قیمت پایه: 1.2 میلیون
   - 5% تخفیف
   - 620 سفارش

4. **محصولات بسته‌بندی**:
   - قیمت پایه: 50 هزار
   - 10% تخفیف
   - 450 سفارش

5. **انبار**:
   - قیمت پایه: 5 میلیون
   - 180 سفارش

6. **خرده‌بار**:
   - قیمت پایه: 800 هزار
   - 520 سفارش

7. **یک یا دو قلم جنس بیشتر ندارم**:
   - قیمت پایه: 600 هزار
   - 720 سفارش

## 🛠 کامپوننت‌های استفاده شده

### از Shadcn/UI:
- Button
- Input
- Card, CardContent, CardHeader, CardTitle, CardDescription
- Table, TableBody, TableCell, TableHead, TableHeader, TableRow
- Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle
- DropdownMenu و اجزای آن
- Badge
- Select, SelectContent, SelectItem, SelectTrigger, SelectValue
- Label
- Tabs, TabsContent, TabsList, TabsTrigger
- Switch
- Textarea
- Separator
- ScrollArea

### کامپوننت‌های سفارشی:
- ImageUpload (از components/common)

### از Lucide React:
- همه آیکون‌های لیست شده در بالا

## 💡 نکات مهم

### اعتبارسنجی:
- نام، slug و توضیحات اجباری هستند (با علامت *)
- درصد تخفیف باید بین 0 تا 100 باشد
- قیمت‌ها باید عدد مثبت باشند
- ویژگی‌های خالی فیلتر می‌شوند

### مرتب‌سازی:
- خدمات بر اساس فیلد "order" مرتب می‌شوند
- عدد کمتر = اولویت بالاتر

### فیلترینگ:
- جستجو case-insensitive است
- فیلتر وضعیت و جستجو با هم ترکیب می‌شوند

### Performance:
- از useMemo برای محاسبات سنگین استفاده نشده (در نسخه بعدی)
- فیلترینگ در هر render انجام می‌شود

## 🔮 توسعه‌های آینده

### پیشنهادات:
1. **اتصال به API**: جایگزینی mock data با API واقعی
2. **آپلود تصویر**: پیاده‌سازی آپلود واقعی تصویر
3. **صفحه‌بندی**: برای لیست‌های طولانی
4. **فیلتر پیشرفته**: فیلتر بر اساس قیمت، امتیاز، تعداد سفارش
5. **مرتب‌سازی**: امکان مرتب‌سازی جدول
6. **نمودارها**: نمودار روند سفارشات هر خدمت
7. **Drag & Drop**: تغییر ترتیب با کشیدن
8. **Export**: خروجی Excel/CSV
9. **Import**: ورود دسته‌جمعی خدمات
10. **تاریخچه تغییرات**: ثبت همه تغییرات
11. **SEO**: مدیریت متا تگ‌ها برای هر خدمت
12. **چندزبانه**: پشتیبانی از زبان‌های مختلف

## 📝 یادداشت‌های توسعه

### برای اتصال به Backend:
```typescript
// GET all services
const fetchServices = async () => {
  const response = await fetch('/api/services');
  const data = await response.json();
  setServices(data);
};

// POST new service
const createService = async (serviceData) => {
  const response = await fetch('/api/services', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(serviceData),
  });
  return response.json();
};

// PUT update service
const updateService = async (id, serviceData) => {
  const response = await fetch(`/api/services/${id}`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(serviceData),
  });
  return response.json();
};

// DELETE service
const deleteService = async (id) => {
  await fetch(`/api/services/${id}`, { method: 'DELETE' });
};
```

### آپلود تصویر:
```typescript
const uploadImage = async (file: File) => {
  const formData = new FormData();
  formData.append('image', file);
  
  const response = await fetch('/api/upload', {
    method: 'POST',
    body: formData,
  });
  
  const { url } = await response.json();
  return url;
};
```

## ✅ چک‌لیست تست

- [ ] افزودن خدمت جدید
- [ ] ویرایش خدمت موجود
- [ ] حذف خدمت
- [ ] تغییر وضعیت فعال/غیرفعال
- [ ] تغییر وضعیت ویژه
- [ ] جستجو در خدمات
- [ ] فیلتر بر اساس وضعیت
- [ ] مشاهده جزئیات خدمت
- [ ] افزودن ویژگی
- [ ] حذف ویژگی
- [ ] آپلود تصویر
- [ ] حذف تصویر
- [ ] اعتبارسنجی فرم
- [ ] پیام‌های toast
- [ ] نمایش صحیح آمار
- [ ] Responsive در موبایل
- [ ] مرتب‌سازی بر اساس order

---

**نسخه**: 1.0.0  
**آخرین بروزرسانی**: 8 نوامبر 2025  
**توسعه‌دهنده**: باربری بهار - تیم فنی
