# صفحه مدیریت کاتالوگ (AdminCatalog)

صفحه جامع برای مدیریت دسته‌بندی‌ها، آیتم‌های کاتالوگ و محصولات بسته‌بندی.

---

## 📋 نمای کلی

این صفحه به ادمین امکان می‌دهد تا:
- دسته‌بندی‌های کاتالوگ را مدیریت کند
- آیتم‌های سنگین (اقلام حمل) را مدیریت کند
- محصولات بسته‌بندی را با تصویر مدیریت کند

---

## 🎯 ویژگی‌های اصلی

### 1️⃣ مدیریت دسته‌بندی‌های کاتالوگ

**عملیات:**
- ✅ افزودن دسته‌بندی جدید
- ✅ ویرایش دسته‌بندی موجود
- ✅ حذف دسته‌بندی (با بررسی وابستگی)
- ✅ جستجو در دسته‌بندی‌ها

**فیلدها:**
- نام دسته‌بندی (الزامی)
- Slug (الزامی) - برای URL
- توضیحات
- ترتیب نمایش

**بررسی‌های امنیتی:**
- نمی‌توان دسته‌بندی‌ای را حذف کرد که آیتم دارد
- Slug باید یکتا باشد

---

### 2️⃣ مدیریت آیتم‌های کاتالوگ

**عملیات:**
- ✅ افزودن آیتم جدید
- ✅ ویرایش آیتم موجود
- ✅ حذف آیتم
- ✅ فعال/غیرفعال کردن آیتم
- ✅ جستجو در آیتم‌ها
- ✅ فیلتر بر اساس دسته‌بندی

**فیلدها:**
- نام آیتم (الزامی)
- دسته‌بندی (الزامی)
- توضیحات
- قیمت پایه (تومان) - الزامی
- واحد (عدد، کیلوگرم، متر، ...)
- ترتیب نمایش
- وضعیت فعال/غیرفعال

**مثال‌های آیتم:**
- پیانو - 750,000 تومان
- گاوصندوق 120 کیلوگرم - 400,000 تومان
- یخچال ساید بای ساید - 500,000 تومان

---

### 3️⃣ مدیریت محصولات بسته‌بندی

**عملیات:**
- ✅ افزودن محصول جدید با تصویر
- ✅ ویرایش محصول موجود
- ✅ حذف محصول
- ✅ فعال/غیرفعال کردن محصول
- ✅ جستجو در محصولات
- ✅ آپلود و مدیریت تصویر محصول

**فیلدها:**
- نام محصول (الزامی)
- توضیحات
- **تصویر محصول** (JPG, PNG, WEBP - حداکثر 2MB)
- قیمت (تومان) - الزامی
- واحد (عدد، رول، بسته، کیلوگرم)
- موجودی
- وضعیت فعال/غیرفعال

**مثال‌های محصول:**
- کارتن بزرگ (50×50×60) - 45,000 تومان
- چسب قهوه‌ای بسته‌بندی - 15,000 تومان
- پلاستیک حباب‌دار (10 متری) - 50,000 تومان

---

## 🖼️ آپلود تصویر

### قابلیت‌های ImageUpload:

1. **آپلود فایل:**
   - کلیک روی ناحیه آپلود
   - یا Drag & Drop تصویر

2. **فرمت‌های مجاز:**
   - JPG / JPEG
   - PNG
   - WEBP

3. **حداکثر حجم:** 2 مگابایت

4. **پیش‌نمایش:**
   - نمایش تصویر بعد از آپلود
   - دکمه‌های تغییر و حذف در Hover

5. **ذخیره‌سازی:**
   - تبدیل به Base64 برای ذخیره در state
   - در تولید، باید به سرور آپلود شود

---

## 📊 داشبورد آماری

در بالای صفحه، 3 کارت آماری نمایش داده می‌شود:

1. **تعداد دسته‌بندی‌ها:** تمام دسته‌بندی‌های فعال
2. **تعداد آیتم‌ها:** آیتم‌های فعال / کل آیتم‌ها
3. **محصولات بسته‌بندی:** محصولات فعال / کل محصولات

---

## 🎨 رابط کاربری

### تب‌ها:
- دسته‌بندی‌ها
- آیتم‌های کاتالوگ
- محصولات بسته‌بندی

### جدول:
- نمایش اطلاعات به صورت جدول
- ستون عملیات با Dropdown Menu
- نمایش تصویر در جدول محصولات
- Badge برای وضعیت و دسته‌بندی
- Switch برای فعال/غیرفعال کردن

### دیالوگ‌ها:
- دیالوگ افزودن/ویرایش دسته‌بندی
- دیالوگ افزودن/ویرایش آیتم
- دیالوگ افزودن/ویرایش محصول (با آپلود تصویر)

### جستجو و فیلتر:
- جستجو در هر تب
- فیلتر آیتم‌ها بر اساس دسته‌بندی

---

## 🔧 ساختار کد

### State Management:

```typescript
// Data
const [categories, setCategories] = useState<CatalogCategory[]>([]);
const [items, setItems] = useState<CatalogItem[]>([]);
const [products, setProducts] = useState<PackingProduct[]>([]);

// Search & Filters
const [categorySearch, setCategorySearch] = useState('');
const [itemSearch, setItemSearch] = useState('');
const [productSearch, setProductSearch] = useState('');
const [selectedCategory, setSelectedCategory] = useState<string>('all');

// Dialogs
const [categoryDialogOpen, setCategoryDialogOpen] = useState(false);
const [itemDialogOpen, setItemDialogOpen] = useState(false);
const [productDialogOpen, setProductDialogOpen] = useState(false);
const [dialogMode, setDialogMode] = useState<DialogMode>(null);

// Forms
const [categoryForm, setCategoryForm] = useState<CategoryFormData>({...});
const [itemForm, setItemForm] = useState<ItemFormData>({...});
const [productForm, setProductForm] = useState<ProductFormData>({...});
```

### توابع اصلی:

```typescript
// Category Functions
openCategoryDialog(mode, category?)
handleCategorySubmit()
handleCategoryDelete(id)

// Item Functions
openItemDialog(mode, item?)
handleItemSubmit()
handleItemDelete(id)
handleItemToggleActive(id)

// Product Functions
openProductDialog(mode, product?)
handleProductSubmit()
handleProductDelete(id)
handleProductToggleActive(id)
```

---

## 📱 Responsive Design

- Mobile: جدول با Scroll افقی
- Tablet: 2 ستون در فرم‌ها
- Desktop: چیدمان کامل

---

## 🔐 اعتبارسنجی

### دسته‌بندی:
- نام و Slug الزامی
- Slug فقط حروف انگلیسی، اعداد و خط تیره

### آیتم:
- نام، دسته‌بندی و قیمت الزامی
- قیمت باید بزرگتر از 0 باشد

### محصول:
- نام و قیمت الزامی
- قیمت باید بزرگتر از 0 باشد
- تصویر اختیاری (فرمت و حجم چک می‌شود)

---

## 🚀 استفاده

1. **افزودن دسته‌بندی:**
   - تب "دسته‌بندی‌ها" → دکمه "دسته‌بندی جدید"
   - پر کردن فرم → "ایجاد دسته‌بندی"

2. **افزودن آیتم:**
   - تب "آیتم‌های کاتالوگ" → دکمه "آیتم جدید"
   - انتخاب دسته‌بندی، پر کردن فرم → "ایجاد آیتم"

3. **افزودن محصول با تصویر:**
   - تب "محصولات بسته‌بندی" → دکمه "محصول جدید"
   - آپلود تصویر (کلیک یا Drag & Drop)
   - پر کردن فرم → "ایجاد محصول"

4. **ویرایش:**
   - کلیک روی منوی 3 نقطه → "ویرایش"

5. **حذف:**
   - کلیک روی منوی 3 نقطه → "حذف"

---

## 🎯 یادداشت‌های مهم

1. **تصاویر به صورت Base64 ذخیره می‌شوند** - در production باید به سرور آپلود شوند
2. **دسته‌بندی‌های دارای آیتم قابل حذف نیستند**
3. **موجودی کمتر از 10 با رنگ قرمز نمایش داده می‌شود**
4. **تمام عملیات با Toast Notification بازخورد دارند**

---

## 🔮 قابلیت‌های آینده (پیشنهادی)

- [ ] Drag & Drop برای تغییر ترتیب آیتم‌ها
- [ ] Export داده‌ها به Excel
- [ ] Import آیتم‌ها از CSV
- [ ] گالری تصاویر برای محصولات (چند تصویر)
- [ ] تاریخچه تغییرات قیمت
- [ ] کپی کردن آیتم
- [ ] Bulk Actions (حذف/فعال‌سازی گروهی)
