# مستندات صفحه مدیریت مالی (AdminFinancial.tsx)

## 📋 خلاصه
صفحه جامع مدیریت مالی سیستم باربری بهار که امکان مشاهده تراکنش‌ها، تسویه‌حساب رانندگان، گزارشات مالی و نمودارهای تحلیلی را فراهم می‌کند. این صفحه یکی از مهم‌ترین بخش‌های پنل ادمین است که کنترل کامل جریان مالی سیستم را در اختیار مدیر قرار می‌دهد.

## 🎯 امکانات اصلی

### 1. کارت‌های آماری (Financial Stats)
چهار کارت آماری در بالای صفحه:

#### کل درآمد:
- نمایش مجموع درآمد از پرداخت‌های سفارشات تکمیل شده
- درآمد امروز
- درصد رشد نسبت به دوره قبل (+12.5%)
- آیکون: DollarSign
- رنگ رشد: سبز با TrendingUp

#### کمیسیون:
- مجموع کمیسیون دریافت شده از سفارشات
- درصد از کل درآمد
- درصد رشد کمیسیون
- آیکون: Wallet
- محاسبه خودکار از تراکنش‌ها

#### پرداخت به رانندگان:
- مجموع پرداخت‌های انجام شده به رانندگان
- تعداد تسویه‌های تکمیل شده
- مبلغ تسویه‌های در انتظار
- آیکون: CreditCard
- اطلاعات مبلغ pending با رنگ آبی

#### تراکنش‌های امروز:
- تعداد تراکنش‌های ثبت شده امروز
- تعداد تسویه‌های در انتظار بررسی
- وضعیت نیاز به بررسی
- آیکون: Receipt
- هشدار زرد برای pending items

### 2. نمودارها (Charts)

#### نمودار خطی - روند درآمد و هزینه:
- **نوع**: LineChart از recharts
- **محور X**: تاریخ (7 روز گذشته)
- **محور Y**: مبلغ (میلیون تومان)
- **خطوط**:
  - **درآمد** (revenue): آبی (#3b82f6)
  - **کمیسیون** (commission): سبز (#10b981)
  - **پرداخت** (payout): نارنجی (#f59e0b)
- **ابعاد**: 300 پیکسل ارتفاع
- **ویژگی‌ها**:
  - Grid با خطوط نقطه‌چین
  - Tooltip با فرمت فارسی
  - Legend با نام‌های فارسی
  - محور Y با فرمت میلیون
- **کاربرد**: نمایش روند مالی هفتگی

#### نمودار دایره‌ای - توزیع تراکنش‌ها:
- **نوع**: PieChart از recharts
- **داده‌ها**:
  - پرداخت سفارش: 65% (آبی)
  - تسویه راننده: 20% (سبز)
  - کمیسیون: 10% (نارنجی)
  - بازگشت وجه: 5% (قرمز)
- **ابعاد**: 300 پیکسل ارتفاع
- **ویژگی‌ها**:
  - Label با درصد روی هر بخش
  - رنگ‌بندی واضح
  - Tooltip برای جزئیات
- **کاربرد**: تحلیل نوع تراکنش‌ها

### 3. تب‌ها (Tabs)
دو تب اصلی:

#### تب تراکنش‌ها:
- لیست کامل تراکنش‌های مالی
- فیلترهای پیشرفته
- جدول اطلاعات کامل

#### تب تسویه‌حساب رانندگان:
- لیست درخواست‌های تسویه
- Badge قرمز برای pending items
- کارت‌های اطلاعاتی برای هر راننده

### 4. بخش تراکنش‌ها (Transactions Section)

#### فیلترهای تراکنش:
**جستجو**:
- جستجو در شناسه تراکنش (ID)
- جستجو در توضیحات
- جستجو در نام راننده
- جستجو در نام مشتری
- آیکون Search در سمت راست

**فیلتر نوع**:
- همه انواع
- پرداخت سفارش
- تسویه راننده
- کمیسیون
- بازگشت وجه
- واریز
- برداشت

**فیلتر وضعیت**:
- همه وضعیت‌ها
- تکمیل شده
- در انتظار
- ناموفق
- لغو شده

**دکمه فیلتر پیشرفته**:
- باز کردن دیالوگ فیلتر تاریخی
- فیلتر بر اساس مبلغ
- فیلتر بر اساس روش پرداخت

#### جدول تراکنش‌ها:
**ستون شناسه**:
- ID تراکنش
- شماره سفارش (اگر مرتبط باشد)

**ستون نوع**:
- بج outline با label فارسی

**ستون مبلغ**:
- مبلغ اصلی با فرمت فارسی
- کمیسیون (اگر دارد) در خط دوم

**ستون طرفین**:
- نام راننده با آیکون Car
- نام مشتری با آیکون Users

**ستون تاریخ**:
- تاریخ فارسی
- ساعت (HH:MM)

**ستون وضعیت**:
- **تکمیل شده**: بج سبز با CheckCircle2
- **در انتظار**: بج خاکستری با Clock
- **ناموفق**: بج قرمز با XCircle

**ستون عملیات**:
- دکمه مشاهده جزئیات (Eye)

### 5. بخش تسویه‌حساب (Settlements Section)

#### فیلتر تسویه‌ها:
- همه وضعیت‌ها
- در انتظار
- در حال پردازش
- تسویه شده
- رد شده

#### کارت‌های تسویه:
**Header**:
- Avatar راننده
- نام راننده
- شماره تلفن
- بج وضعیت

**محتوا - Grid 6 ستونه**:
- **دوره**: تاریخ شروع تا پایان
- **سفارشات**: تعداد
- **درآمد**: میلیون تومان
- **کمیسیون**: با رنگ قرمز
- **قابل پرداخت**: میلیون تومان
- **عملیات**: دکمه‌های مناسب

**دکمه‌های عملیات**:
- **جزئیات**: همیشه نمایش
- **بررسی**: برای وضعیت PENDING
- **تکمیل پرداخت**: برای وضعیت PROCESSING (دکمه سبز)

### 6. دیالوگ جزئیات تراکنش

#### اطلاعات اصلی (Grid 2x2):
- شناسه تراکنش
- نوع (با Badge)
- شماره سفارش (اگر دارد)
- وضعیت (با Badge)

#### اطلاعات مالی:
- **مبلغ کل**: فرمت فارسی
- **کمیسیون**: اگر دارد، با رنگ قرمز و علامت منفی
- **مبلغ خالص**: بعد از Separator، bold

#### اطلاعات طرفین:
- نام راننده با آیکون Car
- نام مشتری با آیکون Users

#### جزئیات پرداخت:
- توضیحات کامل
- روش پرداخت
- شماره پیگیری (با dir="ltr")

#### تاریخ‌ها (Grid 2 ستونه):
- تاریخ ایجاد + ساعت
- تاریخ تکمیل + ساعت (اگر تکمیل شده)

### 7. دیالوگ جزئیات تسویه

#### Header:
- Avatar بزرگ (64x64)
- نام راننده
- شماره تلفن

#### اطلاعات اصلی (Grid 2x2):
- شناسه تسویه
- دوره (از تاریخ تا تاریخ)
- تعداد سفارشات
- وضعیت (با Badge)

#### اطلاعات مالی:
- **کل درآمد**: فرمت کامل
- **کمیسیون**: قرمز با علامت منفی
- **قابل پرداخت**: بزرگ‌تر، بعد از Separator

#### اطلاعات بانکی:
- شماره کارت (dir="ltr")
- شماره حساب (dir="ltr")

#### تاریخچه (Grid 2 ستونه):
- تاریخ درخواست + ساعت
- تاریخ پردازش + ساعت
- تاریخ پرداخت + ساعت (در 2 ستون)

#### یادداشت:
- نمایش در باکس خاکستری (اگر دارد)

### 8. دیالوگ بررسی تسویه

#### نمایش خلاصه:
- باکس خاکستری با اطلاعات:
  - نام راننده
  - مبلغ قابل پرداخت (بزرگ‌تر)

#### فیلد یادداشت:
- Textarea برای یادداشت مدیر
- اختیاری
- 3 خط

#### دکمه‌های عمل:
- **انصراف**: outline
- **رد درخواست**: destructive (قرمز)
- **تأیید و پردازش**: primary (آبی)

## 📊 ساختار داده

### Transaction Interface
```typescript
interface Transaction {
  id: string;                     // شناسه یکتا
  type: TransactionType;          // نوع تراکنش
  amount: number;                 // مبلغ کل
  commission?: number;            // کمیسیون (15%)
  netAmount?: number;             // مبلغ خالص
  status: TransactionStatus;      // وضعیت
  orderId?: string;               // شماره سفارش
  driverId?: string;              // شناسه راننده
  driverName?: string;            // نام راننده
  customerId?: string;            // شناسه مشتری
  customerName?: string;          // نام مشتری
  description: string;            // توضیحات
  paymentMethod?: string;         // روش پرداخت
  referenceNumber?: string;       // شماره پیگیری
  createdAt: Date;                // تاریخ ایجاد
  completedAt?: Date;             // تاریخ تکمیل
}
```

### DriverSettlement Interface
```typescript
interface DriverSettlement {
  id: string;                     // شناسه یکتا
  driverId: string;               // شناسه راننده
  driverName: string;             // نام راننده
  driverPhone: string;            // تلفن راننده
  period: string;                 // دوره (weekly/monthly)
  fromDate: Date;                 // از تاریخ
  toDate: Date;                   // تا تاریخ
  totalOrders: number;            // تعداد سفارشات
  totalEarnings: number;          // کل درآمد
  totalCommission: number;        // کل کمیسیون
  netAmount: number;              // مبلغ خالص
  status: SettlementStatus;       // وضعیت
  bankAccount?: string;           // شماره حساب
  cardNumber?: string;            // شماره کارت
  requestedAt: Date;              // تاریخ درخواست
  processedAt?: Date;             // تاریخ پردازش
  paidAt?: Date;                  // تاریخ پرداخت
  note?: string;                  // یادداشت
}
```

### Enums

#### TransactionType
```typescript
enum TransactionType {
  ORDER_PAYMENT = 'ORDER_PAYMENT',       // پرداخت سفارش
  DRIVER_PAYOUT = 'DRIVER_PAYOUT',       // تسویه راننده
  COMMISSION = 'COMMISSION',             // کمیسیون
  REFUND = 'REFUND',                     // بازگشت وجه
  WITHDRAWAL = 'WITHDRAWAL',             // برداشت
  DEPOSIT = 'DEPOSIT',                   // واریز
}
```

#### TransactionStatus
```typescript
enum TransactionStatus {
  PENDING = 'PENDING',         // در انتظار
  COMPLETED = 'COMPLETED',     // تکمیل شده
  FAILED = 'FAILED',           // ناموفق
  CANCELLED = 'CANCELLED',     // لغو شده
}
```

#### SettlementStatus
```typescript
enum SettlementStatus {
  PENDING = 'PENDING',             // در انتظار
  PROCESSING = 'PROCESSING',       // در حال پردازش
  COMPLETED = 'COMPLETED',         // تسویه شده
  REJECTED = 'REJECTED',           // رد شده
}
```

## 📦 Mock Data

### mockTransactions (8 تراکنش):

1. **TRX-001** - پرداخت سفارش:
   - 2.5 میلیون، کمیسیون 375 هزار
   - تکمیل شده، مشتری: علی احمدی
   - راننده: محمد رضایی

2. **TRX-002** - تسویه راننده:
   - 5 میلیون
   - تکمیل شده، تسویه هفتگی

3. **TRX-003** - پرداخت سفارش:
   - 1.8 میلیون، کمیسیون 270 هزار
   - تکمیل شده

4. **TRX-004** - کمیسیون:
   - 645 هزار
   - کمیسیون روزانه

5. **TRX-005** - بازگشت وجه:
   - 1.5 میلیون
   - لغو سفارش

6. **TRX-006** - پرداخت سفارش:
   - 3.2 میلیون
   - اسباب‌کشی اداری

7. **TRX-007** - پرداخت سفارش:
   - 2.2 میلیون
   - در انتظار (PENDING)

8. **TRX-008** - تسویه راننده:
   - 7.5 میلیون
   - در حال پردازش (PROCESSING)

### mockSettlements (4 تسویه):

1. **STL-001** - محمد رضایی:
   - 25 سفارش، 15 میلیون درآمد
   - تسویه شده، هفته اول آبان

2. **STL-002** - حسین موسوی:
   - 18 سفارش، 11 میلیون درآمد
   - تسویه شده

3. **STL-003** - رضا کریمی:
   - 22 سفارش، 13.5 میلیون درآمد
   - در حال پردازش

4. **STL-004** - علی اکبری:
   - 15 سفارش، 9 میلیون درآمد
   - در انتظار

### Chart Data

#### revenueChartData (7 روز):
- روزانه: revenue, commission, payout
- محدوده: 12.5 تا 22 میلیون

#### transactionTypePieData:
- پرداخت سفارش: 65%
- تسویه راننده: 20%
- کمیسیون: 10%
- بازگشت وجه: 5%

## 🎨 ویژگی‌های UI/UX

### رنگ‌بندی:
- **درآمد**: آبی (#3b82f6)
- **کمیسیون**: سبز (#10b981)
- **پرداخت**: نارنجی (#f59e0b)
- **بازگشت وجه**: قرمز (#ef4444)
- **رشد مثبت**: text-green-600
- **رشد منفی**: text-red-600
- **هشدار**: text-amber-600

### بج‌ها:
- **تکمیل شده**: bg-green-500 با CheckCircle2
- **در انتظار**: secondary با Clock
- **در حال پردازش**: default با AlertCircle
- **ناموفق/رد شده**: destructive با XCircle

### آیکون‌ها:
از lucide-react:
- DollarSign, Wallet, CreditCard, Receipt
- TrendingUp, TrendingDown
- ArrowUpRight, ArrowDownRight
- CheckCircle2, XCircle, Clock, AlertCircle
- Eye, Download, Upload, FileText
- Search, Filter, MoreVertical
- Car, Users, Building, Banknote

### Responsive:
- کارت‌های آمار: 4 ستون در دسکتاپ
- نمودارها: 4+3 ستون در دسکتاپ
- فیلترها: 5 ستون در دسکتاپ
- جدول: scroll افقی در موبایل

## 🔄 عملیات (Handlers)

### handleViewTransaction:
- ذخیره تراکنش انتخاب شده
- باز کردن دیالوگ جزئیات

### handleViewSettlement:
- ذخیره تسویه انتخاب شده
- باز کردن دیالوگ جزئیات تسویه

### handleProcessSettlement:
- ذخیره تسویه انتخاب شده
- ریست یادداشت
- باز کردن دیالوگ بررسی

### confirmProcessSettlement:
- **approve**: تغییر وضعیت به PROCESSING
- **reject**: تغییر وضعیت به REJECTED
- ذخیره processedAt و note
- نمایش toast موفقیت

### completeSettlement:
- تغییر وضعیت به COMPLETED
- ذخیره paidAt
- نمایش toast موفقیت

### formatCurrency:
- فرمت اعداد به فارسی
- با جداکننده هزارگان
- استفاده: `Intl.NumberFormat('fa-IR')`

## 📈 محاسبات آماری

### stats.totalRevenue:
```typescript
transactions
  .filter(t => 
    t.type === TransactionType.ORDER_PAYMENT && 
    t.status === TransactionStatus.COMPLETED
  )
  .reduce((sum, t) => sum + t.amount, 0)
```

### stats.totalCommission:
```typescript
transactions
  .filter(t => 
    t.type === TransactionType.ORDER_PAYMENT && 
    t.status === TransactionStatus.COMPLETED
  )
  .reduce((sum, t) => sum + (t.commission || 0), 0)
```

### stats.totalPayout:
```typescript
transactions
  .filter(t => 
    t.type === TransactionType.DRIVER_PAYOUT && 
    t.status === TransactionStatus.COMPLETED
  )
  .reduce((sum, t) => sum + t.amount, 0)
```

### stats.pendingSettlements:
```typescript
settlements.filter(s => s.status === SettlementStatus.PENDING).length
```

### stats.todayRevenue:
```typescript
transactions
  .filter(t => {
    // فیلتر بر اساس تاریخ امروز
    // و نوع ORDER_PAYMENT
    // و وضعیت COMPLETED
  })
  .reduce((sum, t) => sum + t.amount, 0)
```

## 🔍 فیلترینگ

### filteredTransactions:
ترکیب فیلترهای زیر:
1. **جستجو**: در ID, description, driverName, customerName
2. **نوع**: مطابقت با typeFilter
3. **وضعیت**: مطابقت با statusFilter
4. **تاریخ**: بین dateFrom و dateTo

### filteredSettlements:
فیلتر بر اساس:
- **وضعیت**: مطابقت با settlementStatusFilter

## 🛠 کامپوننت‌های استفاده شده

### از Shadcn/UI:
- Button
- Input
- Card و اجزای آن
- Table و اجزای آن
- Dialog و اجزای آن
- DropdownMenu و اجزای آن
- Badge
- Select و اجزای آن
- Label
- Tabs و اجزای آن
- Separator
- Avatar و اجزای آن
- Textarea

### از Recharts:
- LineChart, Line
- PieChart, Pie, Cell
- BarChart, Bar
- XAxis, YAxis
- CartesianGrid
- Tooltip
- Legend
- ResponsiveContainer

### از Lucide React:
- تمام آیکون‌های لیست شده

## 💼 فلوی تسویه‌حساب

### مراحل تسویه:
```
1. راننده درخواست می‌کند
   ↓ (وضعیت: PENDING)

2. ادمین بررسی می‌کند
   ↓ (handleProcessSettlement)

3. ادمین تأیید یا رد می‌کند
   ↓ (confirmProcessSettlement)
   
   - تأیید → PROCESSING
   - رد → REJECTED

4. ادمین پرداخت را تکمیل می‌کند
   ↓ (completeSettlement)

5. وضعیت: COMPLETED
   ✓ paidAt ثبت می‌شود
```

## 💡 نکات مهم

### درصد کمیسیون:
- پیش‌فرض: 15%
- قابل تنظیم در هر تراکنش
- محاسبه: `commission = amount * 0.15`
- مبلغ خالص: `netAmount = amount - commission`

### تاریخ فارسی:
- استفاده از `toLocaleDateString('fa-IR')`
- فرمت ساعت: `{ hour: '2-digit', minute: '2-digit' }`
- تاریخ کوتاه: `{ month: 'short', day: 'numeric' }`

### فرمت پولی:
- تبدیل به میلیون برای نمایش کوتاه
- استفاده از "م" به جای "میلیون"
- نمایش کامل در دیالوگ‌ها با "تومان"

### وضعیت‌های تراکنش:
- **PENDING**: جدید ایجاد شده، نیاز به پردازش
- **COMPLETED**: پرداخت موفق، completedAt ثبت شده
- **FAILED**: پرداخت ناموفق، نیاز به بررسی
- **CANCELLED**: لغو شده توسط کاربر یا سیستم

### روش‌های پرداخت:
- کارت به کارت
- آنلاین (درگاه)
- واریز کارت
- واریز حساب
- نقدی

## 🔮 توسعه‌های آینده

### پیشنهادات:
1. **اتصال به API**: جایگزینی mock data
2. **Pagination**: برای لیست تراکنش‌ها
3. **فیلتر پیشرفته تاریخ**: Date Range Picker
4. **Export**: خروجی Excel با داده‌های فیلتر شده
5. **گزارش PDF**: صورتحساب قابل چاپ
6. **نمودارهای بیشتر**: 
   - نمودار ستونی مقایسه ماهانه
   - نمودار روند کمیسیون
   - نمودار عملکرد رانندگان
7. **Dashboard مالی پیشرفته**: 
   - پیش‌بینی درآمد
   - تحلیل سود و زیان
   - نرخ تبدیل
8. **اعلان‌ها**: 
   - تسویه جدید
   - تراکنش‌های بزرگ
   - تراکنش‌های مشکوک
9. **یکپارچه‌سازی بانکی**: 
   - پرداخت خودکار
   - تأیید تراکنش
   - رسید دیجیتال
10. **گزارش دوره‌ای**: 
    - روزانه، هفتگی، ماهانه
    - ارسال خودکار ایمیل
    - ذخیره در تاریخچه
11. **مدیریت کمیسیون پیشرفته**:
    - کمیسیون متغیر بر اساس راننده
    - کمیسیون پلکانی
    - بونوس و تخفیف
12. **تسویه خودکار**: 
    - تنظیم دوره تسویه
    - تسویه خودکار در زمان مشخص
    - اعلان به راننده

## 📝 یادداشت‌های توسعه

### برای اتصال به Backend:

```typescript
// GET all transactions
const fetchTransactions = async (filters) => {
  const response = await fetch('/api/financial/transactions', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(filters),
  });
  return response.json();
};

// GET transaction details
const fetchTransaction = async (id) => {
  const response = await fetch(`/api/financial/transactions/${id}`);
  return response.json();
};

// GET all settlements
const fetchSettlements = async (status) => {
  const response = await fetch(`/api/financial/settlements?status=${status}`);
  return response.json();
};

// PATCH process settlement
const processSettlement = async (id, action, note) => {
  const response = await fetch(`/api/financial/settlements/${id}/process`, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ action, note }),
  });
  return response.json();
};

// PATCH complete settlement
const completeSettlement = async (id) => {
  const response = await fetch(`/api/financial/settlements/${id}/complete`, {
    method: 'PATCH',
  });
  return response.json();
};

// GET financial stats
const fetchFinancialStats = async (period) => {
  const response = await fetch(`/api/financial/stats?period=${period}`);
  return response.json();
};

// GET chart data
const fetchChartData = async (type, range) => {
  const response = await fetch(
    `/api/financial/charts?type=${type}&range=${range}`
  );
  return response.json();
};

// POST export transactions
const exportTransactions = async (filters, format) => {
  const response = await fetch('/api/financial/export', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ filters, format }),
  });
  const blob = await response.blob();
  // Download file
};
```

### محاسبه کمیسیون:
```typescript
const calculateCommission = (amount: number, rate: number = 0.15) => {
  return Math.round(amount * rate);
};

const calculateNetAmount = (amount: number, commission: number) => {
  return amount - commission;
};

// برای سفارش
const orderAmount = 2500000;
const commission = calculateCommission(orderAmount);
const driverPayout = calculateNetAmount(orderAmount, commission);

console.log({
  orderAmount,     // 2,500,000
  commission,      // 375,000
  driverPayout,    // 2,125,000
});
```

### تسویه هفتگی خودکار:
```typescript
const calculateWeeklySettlement = async (driverId: string) => {
  const today = new Date();
  const weekAgo = new Date(today.getTime() - 7 * 24 * 60 * 60 * 1000);
  
  // دریافت سفارشات راننده در هفته گذشته
  const orders = await fetchDriverOrders(driverId, weekAgo, today);
  
  const totalOrders = orders.length;
  const totalEarnings = orders.reduce((sum, order) => sum + order.price, 0);
  const totalCommission = orders.reduce((sum, order) => sum + order.commission, 0);
  const netAmount = totalEarnings - totalCommission;
  
  // ایجاد درخواست تسویه
  const settlement: DriverSettlement = {
    id: `STL-${Date.now()}`,
    driverId,
    period: 'weekly',
    fromDate: weekAgo,
    toDate: today,
    totalOrders,
    totalEarnings,
    totalCommission,
    netAmount,
    status: SettlementStatus.PENDING,
    requestedAt: new Date(),
  };
  
  return settlement;
};
```

## ✅ چک‌لیست تست

### تراکنش‌ها:
- [ ] بارگذاری لیست تراکنش‌ها
- [ ] جستجو در تراکنش‌ها
- [ ] فیلتر بر اساس نوع
- [ ] فیلتر بر اساس وضعیت
- [ ] فیلتر بر اساس تاریخ
- [ ] مشاهده جزئیات تراکنش
- [ ] فرمت صحیح مبلغ‌ها
- [ ] نمایش کمیسیون
- [ ] نمایش مبلغ خالص

### تسویه‌حساب:
- [ ] بارگذاری لیست تسویه‌ها
- [ ] فیلتر بر اساس وضعیت
- [ ] مشاهده جزئیات تسویه
- [ ] بررسی و تأیید تسویه
- [ ] رد تسویه با دلیل
- [ ] تکمیل پرداخت
- [ ] نمایش اطلاعات بانکی
- [ ] محاسبه صحیح مبالغ

### آمار و نمودارها:
- [ ] محاسبه کل درآمد
- [ ] محاسبه کمیسیون
- [ ] محاسبه پرداخت‌ها
- [ ] شمارش تراکنش‌های امروز
- [ ] نمودار خطی روند
- [ ] نمودار دایره‌ای توزیع
- [ ] Tooltip نمودارها
- [ ] Responsive نمودارها

### عملکرد:
- [ ] سرعت بارگذاری
- [ ] فیلترینگ سریع
- [ ] جستجوی آنی
- [ ] Pagination (آینده)
- [ ] Export داده‌ها
- [ ] Responsive در موبایل

### UI/UX:
- [ ] رنگ‌بندی صحیح
- [ ] بج‌های مناسب
- [ ] آیکون‌های واضح
- [ ] فرمت تاریخ فارسی
- [ ] Toast notifications
- [ ] Loading states
- [ ] Empty states

---

**نسخه**: 1.0.0  
**آخرین بروزرسانی**: 8 نوامبر 2025  
**توسعه‌دهنده**: باربری بهار - تیم فنی  
**وابستگی‌ها**: recharts, lucide-react, shadcn/ui
