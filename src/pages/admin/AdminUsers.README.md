# مستندات صفحه مدیریت کاربران (AdminUsers.tsx)

## 📋 خلاصه
صفحه جامع مدیریت کاربران سیستم باربری بهار که امکان مدیریت کامل مشتریان، رانندگان و ادمین‌ها را فراهم می‌کند. این صفحه شامل لیست کاربران، فرم افزودن/ویرایش، صفحه جزئیات، مدیریت نقش‌ها، مسدودسازی و قابلیت‌های مدیریتی کامل است.

## 🎯 امکانات اصلی

### 1. کارت‌های آماری (Stats Cards)
چهار کارت آماری در بالای صفحه:

#### کل کاربران:
- نمایش تعداد کل کاربران سیستم
- تعداد کاربران فعال و مسدود
- آیکون: Users

#### مشتریان:
- تعداد کاربران با نقش CUSTOMER
- درصد از کل کاربران
- آیکون: UserCheck

#### رانندگان:
- تعداد کاربران با نقش DRIVER
- درصد از کل کاربران
- آیکون: UserCog

#### کاربران جدید:
- تعداد کاربران ثبت‌نام شده امروز
- تعداد کاربران ثبت‌نام شده این ماه
- آیکون: Activity

### 2. فیلتر و جستجو
بخش جامع فیلتر شامل:

#### جستجو:
- جستجو بر اساس نام کامل
- جستجو در شماره تلفن
- جستجو در ایمیل
- جستجو در شناسه (ID)
- آیکون جستجو در سمت راست Input

#### فیلتر نقش:
- **همه نقش‌ها**: نمایش تمام کاربران
- **مشتریان**: فقط CUSTOMER
- **رانندگان**: فقط DRIVER
- **ادمین‌ها**: فقط ADMIN

#### فیلتر وضعیت:
- **همه وضعیت‌ها**: تمام کاربران
- **فعال**: کاربران فعال و غیرمسدود
- **غیرفعال**: کاربران غیرفعال
- **مسدود**: کاربران مسدود شده

#### دکمه‌های عملیاتی:
- دانلود (Export): خروجی لیست کاربران
- آپلود (Import): ورود دسته‌جمعی

### 3. جدول لیست کاربران
جدول جامع با ستون‌های زیر:

#### ستون کاربر:
- Avatar با حرف اول نام
- نام و نام خانوادگی
- شناسه کاربر (ID)

#### ستون نقش:
- بج نقش با رنگ متمایز:
  - **ادمین**: قرمز (destructive)
  - **راننده**: آبی (default)
  - **مشتری**: خاکستری (secondary)

#### ستون تماس:
- شماره تلفن با آیکون Phone
- ایمیل (اگر دارد) با آیکون Mail

#### ستون آمار:
- تعداد کل سفارشات
- برای مشتری: مبلغ خرج شده (میلیون تومان)
- برای راننده: درآمد کل (میلیون تومان)

#### ستون آخرین ورود:
- تاریخ به فارسی
- ساعت (HH:MM)

#### ستون وضعیت:
- بج فعال/غیرفعال/مسدود
- بج‌های کوچک برای تأیید تلفن و ایمیل

#### ستون عملیات:
منوی Dropdown شامل:
- **مشاهده جزئیات**: نمایش دیالوگ اطلاعات کامل
- **ویرایش**: باز کردن فرم ویرایش
- **فعال/غیرفعال کردن**: تغییر وضعیت
- **مسدود کردن / رفع مسدودیت**: مدیریت مسدودسازی
- **حذف**: حذف کاربر (با رنگ قرمز)

### 4. فرم افزودن/ویرایش کاربر
فرم دوتبه با امکانات کامل:

#### تب 1: اطلاعات پایه
- **نام و نام خانوادگی** (اجباری): نام کامل کاربر
- **شماره تلفن** (اجباری): شماره موبایل (11 رقم)
- **ایمیل**: آدرس ایمیل (اختیاری)
- **نقش کاربری** (اجباری): Select بین مشتری/راننده/ادمین
- **کد ملی**: کد ملی 10 رقمی
- **شهر**: نام شهر محل سکونت
- **آدرس**: Textarea برای آدرس کامل

#### تب 2: تنظیمات
- **وضعیت فعال/غیرفعال**: Switch برای فعال/غیرفعال کردن ورود
- **یادداشت ادمین**: Textarea برای یادداشت‌های داخلی

### 5. دیالوگ جزئیات کاربر
صفحه کامل نمایش اطلاعات شامل:

#### Header:
- Avatar بزرگ (64x64 پیکسل)
- نام کامل
- شناسه کاربر
- بج نقش و وضعیت

#### اطلاعات تماس:
Grid دو ستونه شامل:
- **شماره تلفن**: با آیکون + بج تأیید (اگر تأیید شده)
- **ایمیل**: با آیکون + بج تأیید (اگر دارد)
- **کد ملی**: (اگر دارد)
- **شهر**: با آیکون MapPin (اگر دارد)
- **آدرس کامل**: در 2 ستون (اگر دارد)

#### آمار عملکرد:
سه کارت جداگانه:
- **کل سفارشات**: تعداد کل
- **تکمیل شده**: تعداد + درصد نرخ تکمیل
- **لغو شده**: تعداد + درصد نرخ لغو

**کارت اضافی برای مشتری**:
- کل مبلغ خرج شده (میلیون تومان)

**کارت اضافی برای راننده**:
- کل درآمد (میلیون تومان)

#### اطلاعات مسدودیت:
(فقط برای کاربران مسدود شده)
- باکس قرمز با border و background قرمز روشن
- تاریخ مسدودیت
- دلیل مسدودیت

#### یادداشت ادمین:
- نمایش یادداشت (اگر دارد) در باکس خاکستری

#### تاریخ‌ها:
Grid سه ستونه:
- **تاریخ عضویت**: با آیکون Calendar
- **آخرین ورود**: با آیکون Clock (تاریخ + ساعت)
- **آخرین بروزرسانی**: (اگر دارد)

### 6. دیالوگ حذف
- نمایش نام کاربر
- هشدار عدم بازگشت‌پذیری
- دکمه‌های انصراف و حذف

### 7. دیالوگ مسدودسازی
دو حالت:

#### حالت مسدود کردن:
- فیلد دلیل مسدودیت (اجباری)
- Textarea برای وارد کردن دلیل
- دکمه "مسدود کردن" قرمز

#### حالت رفع مسدودیت:
- پیام تأیید رفع مسدودیت
- دکمه "رفع مسدودیت" آبی

## 📊 ساختار داده

### Interface User
```typescript
interface User {
  id: string;                    // شناسه یکتا
  phoneNumber: string;           // شماره تلفن (اجباری)
  fullName: string;              // نام کامل
  email?: string;                // ایمیل (اختیاری)
  role: UserRole;                // نقش کاربری
  
  // اطلاعات تکمیلی
  nationalId?: string;           // کد ملی
  address?: string;              // آدرس
  city?: string;                 // شهر
  
  // وضعیت
  isActive: boolean;             // فعال/غیرفعال
  isEmailVerified: boolean;      // تأیید ایمیل
  isPhoneVerified: boolean;      // تأیید تلفن
  isBanned: boolean;             // مسدود/غیرمسدود
  
  // آمار
  totalOrders: number;           // کل سفارشات
  completedOrders: number;       // تکمیل شده
  cancelledOrders: number;       // لغو شده
  totalSpent: number;            // خرج (مشتری)
  totalEarnings: number;         // درآمد (راننده)
  
  // تاریخ‌ها
  lastLoginAt?: Date;            // آخرین ورود
  createdAt: Date;               // تاریخ عضویت
  updatedAt?: Date;              // آخرین بروزرسانی
  bannedAt?: Date;               // تاریخ مسدودیت
  
  // یادداشت
  adminNote?: string;            // یادداشت ادمین
  banReason?: string;            // دلیل مسدودیت
}
```

### UserRole Enum
```typescript
enum UserRole {
  GUEST = 'GUEST',
  CUSTOMER = 'CUSTOMER',
  DRIVER = 'DRIVER',
  ADMIN = 'ADMIN',
}
```

### Role Labels
```typescript
const ROLE_LABELS: Record<UserRole, string> = {
  GUEST: 'مهمان',
  CUSTOMER: 'مشتری',
  DRIVER: 'راننده',
  ADMIN: 'ادمین',
};
```

### Role Colors
```typescript
const ROLE_COLORS: Record<UserRole, string> = {
  GUEST: 'gray',
  CUSTOMER: 'blue',
  DRIVER: 'green',
  ADMIN: 'red',
};
```

## 🎨 ویژگی‌های UI/UX

### بج‌ها (Badges):
- **نقش ادمین**: variant="destructive" (قرمز)
- **نقش راننده**: variant="default" (آبی)
- **نقش مشتری**: variant="secondary" (خاکستری)
- **فعال**: variant="default" (آبی/سبز)
- **غیرفعال**: variant="secondary" (خاکستری)
- **مسدود**: variant="destructive" با آیکون Ban (قرمز)
- **تأیید شده**: variant="outline" با رنگ سبز

### رنگ‌بندی:
- عملیات حذف: text-red-600
- دیالوگ مسدودیت: border-red-200, bg-red-50
- متن کمک‌رسان: text-muted-foreground
- آیکون‌ها: h-4 w-4 text-muted-foreground

### آیکون‌ها:
از lucide-react استفاده شده:
- Plus, Search, Filter, MoreVertical
- Edit, Trash2, Eye
- Users, UserCheck, UserX, Shield
- Phone, Mail, Calendar, MapPin
- Activity, Clock
- CheckCircle2, XCircle, Ban, UserCog
- Download, Upload

### Responsive:
- کارت‌های آمار: 4 ستون در دسکتاپ، تک ستون در موبایل
- فیلترها: ردیف در دسکتاپ، ستون در موبایل
- فرم‌ها: 2 ستون در دسکتاپ، تک ستون در موبایل
- جدول جزئیات: 3 ستون در دسکتاپ، تک ستون در موبایل

## 🔄 عملیات (Handlers)

### handleAddUser:
- ریست کردن فرم به مقادیر پیش‌فرض
- باز کردن دیالوگ افزودن
- نقش پیش‌فرض: CUSTOMER

### handleEditUser:
- پر کردن فرم با اطلاعات کاربر انتخاب شده
- باز کردن دیالوگ ویرایش

### handleSaveUser:
- چک کردن حالت (افزودن یا ویرایش)
- برای کاربر جدید:
  - isPhoneVerified = true (پیش‌فرض)
  - isEmailVerified = false
  - isBanned = false
  - آمار صفر
- به‌روزرسانی updatedAt
- نمایش toast موفقیت

### confirmDeleteUser:
- حذف کاربر از لیست
- نمایش toast موفقیت

### handleBanUser:
- ذخیره کاربر انتخاب شده
- ریست دلیل مسدودیت
- باز کردن دیالوگ مسدودسازی

### confirmBanUser:
- Toggle وضعیت isBanned
- اگر مسدود می‌شود:
  - ذخیره bannedAt
  - ذخیره banReason
- اگر رفع مسدودیت می‌شود:
  - پاک کردن bannedAt
  - پاک کردن banReason
- نمایش toast مناسب

### toggleUserStatus:
- تغییر isActive
- به‌روزرسانی updatedAt
- نمایش toast

### changeUserRole:
- تغییر role کاربر
- به‌روزرسانی updatedAt
- نمایش toast

## 📦 Mock Data

فایل شامل 8 کاربر نمونه:

1. **علی احمدی** (مشتری):
   - 15 سفارش، 25 میلیون خرج
   - فعال، تلفن و ایمیل تأیید شده

2. **محمد رضایی** (راننده):
   - 120 سفارش، 85 میلیون درآمد
   - فعال، تأیید کامل

3. **فاطمه کریمی** (مشتری):
   - 8 سفارش، ایمیل تأیید نشده
   - فعال

4. **مدیر سیستم** (ادمین):
   - صفر سفارش
   - فعال، تأیید کامل

5. **حسین موسوی** (راننده):
   - 95 سفارش، 65 میلیون درآمد
   - فعال

6. **زهرا محمدی** (مشتری):
   - 5 سفارش، 3 لغو
   - مسدود با دلیل "لغو مکرر سفارشات"
   - یادداشت ادمین دارد

7. **رضا اکبری** (مشتری):
   - 3 سفارش
   - فعال، ایمیل ندارد

8. **مهدی حسینی** (راننده):
   - 75 سفارش، 52 میلیون درآمد
   - فعال

## 🛠 کامپوننت‌های استفاده شده

### از Shadcn/UI:
- Button
- Input
- Card, CardContent, CardHeader, CardTitle, CardDescription
- Table, TableBody, TableCell, TableHead, TableHeader, TableRow
- Dialog و اجزای آن
- DropdownMenu و اجزای آن
- Badge
- Select و اجزای آن
- Label
- Tabs, TabsContent, TabsList, TabsTrigger
- Switch
- Textarea
- Separator
- Avatar, AvatarFallback, AvatarImage

### از Lucide React:
- همه آیکون‌های لیست شده در بالا

## 💡 نکات مهم

### اعتبارسنجی:
- شماره تلفن و نام اجباری هستند
- شماره تلفن باید 11 رقم باشد
- کد ملی باید 10 رقم باشد
- ایمیل باید فرمت صحیح داشته باشد
- در حالت مسدودسازی، دلیل اجباری است

### فیلترینگ:
- جستجو case-insensitive است
- فیلترهای نقش، وضعیت و جستجو با هم ترکیب می‌شوند
- فیلترینگ در هر render انجام می‌شود

### محاسبات آماری:
- کاربران امروز: بر اساس تاریخ createdAt
- کاربران این ماه: بر اساس ماه و سال createdAt
- درصدها با یک رقم اعشار نمایش داده می‌شوند

### مسدودسازی:
- کاربر مسدود نمی‌تواند وارد سیستم شود
- دلیل مسدودیت ذخیره می‌شود
- تاریخ مسدودیت ثبت می‌شود
- رفع مسدودیت اطلاعات ban را پاک می‌کند

## 🔮 توسعه‌های آینده

### پیشنهادات:
1. **اتصال به API**: جایگزینی mock data با API واقعی
2. **Pagination**: برای لیست‌های طولانی
3. **Export**: خروجی Excel/CSV
4. **Import**: ورود دسته‌جمعی کاربران
5. **فیلتر پیشرفته**: بر اساس تاریخ، شهر، آمار
6. **مرتب‌سازی**: امکان مرتب‌سازی ستون‌ها
7. **ارسال ایمیل/SMS**: به کاربران منتخب
8. **تاریخچه فعالیت**: لاگ عملیات کاربر
9. **مدیریت مجوزها**: برای ادمین‌ها
10. **نمودارها**: روند ثبت‌نام، فعالیت کاربران
11. **Bulk Actions**: عملیات دسته‌جمعی
12. **نوتیفیکیشن**: اطلاع‌رسانی تغییرات

## 📝 یادداشت‌های توسعه

### برای اتصال به Backend:
```typescript
// GET all users
const fetchUsers = async () => {
  const response = await fetch('/api/users');
  const data = await response.json();
  setUsers(data);
};

// POST new user
const createUser = async (userData) => {
  const response = await fetch('/api/users', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(userData),
  });
  return response.json();
};

// PUT update user
const updateUser = async (id, userData) => {
  const response = await fetch(`/api/users/${id}`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(userData),
  });
  return response.json();
};

// DELETE user
const deleteUser = async (id) => {
  await fetch(`/api/users/${id}`, { method: 'DELETE' });
};

// PATCH ban user
const banUser = async (id, banReason) => {
  const response = await fetch(`/api/users/${id}/ban`, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ banReason }),
  });
  return response.json();
};

// PATCH unban user
const unbanUser = async (id) => {
  const response = await fetch(`/api/users/${id}/unban`, {
    method: 'PATCH',
  });
  return response.json();
};
```

### Validation نمونه:
```typescript
const validateUserForm = (form) => {
  const errors = [];
  
  if (!form.fullName || form.fullName.trim().length < 3) {
    errors.push('نام باید حداقل 3 کاراکتر باشد');
  }
  
  if (!form.phoneNumber || !/^09\d{9}$/.test(form.phoneNumber)) {
    errors.push('شماره تلفن نامعتبر است');
  }
  
  if (form.email && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email)) {
    errors.push('ایمیل نامعتبر است');
  }
  
  if (form.nationalId && !/^\d{10}$/.test(form.nationalId)) {
    errors.push('کد ملی باید 10 رقم باشد');
  }
  
  return errors;
};
```

## ✅ چک‌لیست تست

- [ ] بارگذاری صفحه و نمایش آمار
- [ ] جستجو در کاربران
- [ ] فیلتر بر اساس نقش
- [ ] فیلتر بر اساس وضعیت
- [ ] ترکیب فیلترها و جستجو
- [ ] افزودن کاربر جدید
- [ ] ویرایش کاربر موجود
- [ ] حذف کاربر
- [ ] مشاهده جزئیات
- [ ] تغییر وضعیت فعال/غیرفعال
- [ ] مسدود کردن کاربر
- [ ] رفع مسدودیت
- [ ] تغییر نقش کاربر
- [ ] نمایش بج‌های تأیید
- [ ] محاسبه صحیح آمار
- [ ] فرمت تاریخ فارسی
- [ ] Responsive در موبایل
- [ ] Toast notifications

---

**نسخه**: 1.0.0  
**آخرین بروزرسانی**: 8 نوامبر 2025  
**توسعه‌دهنده**: باربری بهار - تیم فنی
